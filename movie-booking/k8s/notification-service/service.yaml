apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: movie-booking
spec:
  selector:
    app: notification-service
  ports:
    - port: 8080
      targetPort: 9090