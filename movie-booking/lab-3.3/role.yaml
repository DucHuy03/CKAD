apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: movie-reader-role
  namespace: lab

rules:
- apiGroups: [""]
  resources:
    - pods
  verbs:
    - get
    - list
    - watch