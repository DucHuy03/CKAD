apiVersion: v1
kind: ResourceQuota
metadata:
  name: lab-quota
  namespace: lab

spec:
  hard:
    pods: "10"

    requests.cpu: "2"
    requests.memory: 2Gi

    limits.cpu: "4"
    limits.memory: 4Gi